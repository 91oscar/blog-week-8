export default async function search({ params }) {

    return (
        <div>
            <h1>Search</h1>
            <p>If you want to find a specific post, write the URL + search + the id of the post <br /><br />
                Example: http://localhost:3001/search/1 </p>

        </div>
    );
}
